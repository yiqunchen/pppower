% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/required_n_ppi.R
\name{power_ppi_regression}
\alias{power_ppi_regression}
\title{Power or required sample size for PPI++ regression contrast}
\usage{
power_ppi_regression(
  delta,
  N,
  n = NULL,
  power = NULL,
  alpha = 0.05,
  lambda_mode = c("vanilla", "oracle", "user"),
  lambda_user = NULL,
  c = NULL,
  H_L = NULL,
  H_U = NULL,
  Sigma_YY = NULL,
  Sigma_ff_l = NULL,
  Sigma_ff_u = NULL,
  Sigma_Yf = NULL,
  warn_smallN = TRUE,
  smallN_threshold = 500,
  mode = c("error", "cap")
)
}
\arguments{
\item{delta}{Numeric scalar. Effect size \eqn{a^\top \beta - \theta_0}. Must be nonzero when solving for \code{n}.}

\item{N}{Positive scalar. Number of unlabeled observations.}

\item{n}{Labeled sample size. Set to \code{NULL} to solve for required \code{n}.}

\item{power}{Desired power. Set to \code{NULL} to compute power from \code{n}.}

\item{alpha}{Significance level (default \code{0.05}).}

\item{lambda_mode}{\code{"vanilla"}, \code{"oracle"}, or \code{"user"}.}

\item{lambda_user}{Scalar specifying \eqn{\lambda} when \code{lambda_mode = "user"}.}

\item{c}{Contrast vector \eqn{a}.}

\item{H_L}{Labeled Hessian/Fisher block.}

\item{H_U}{Unlabeled Hessian/Fisher block.}

\item{Sigma_YY}{Labeled covariance block.}

\item{Sigma_ff_l}{Labeled prediction covariance block.}

\item{Sigma_ff_u}{Unlabeled prediction covariance block.}

\item{Sigma_Yf}{Cross-covariance block.}

\item{warn_smallN}{Logical. Warn when \code{N < smallN_threshold}.}

\item{smallN_threshold}{Numeric. Default \code{500}.}

\item{mode}{\code{"error"} or \code{"cap"}.}
}
\value{
When computing power: scalar in [0, 1].
When computing sample size: integer \code{n} required.
}
\description{
Computes the minimum labeled sample size \eqn{n} for a PPI++ regression
contrast, or computes power given \code{n}. Follows the \pkg{pwr} convention:
exactly one of \code{n} or \code{power} must be \code{NULL}.

Supports both OLS and canonical GLM contrasts via sandwich/Fisher blocks
computed on labeled and unlabeled samples.
}
\examples{
p <- 3
cvec <- c(1, 0, -1)
Hpop <- diag(p)
SYY  <- diag(p)
Sff  <- 0.5 * diag(p)
SYf  <- 0.3 * diag(p)

# Compute required sample size
power_ppi_regression(
  delta = 0.15, N = 2000, power = 0.90,
  c = cvec, H_L = Hpop, H_U = Hpop,
  Sigma_YY = SYY, Sigma_ff_l = Sff, Sigma_ff_u = Sff,
  Sigma_Yf = SYf, lambda_mode = "oracle"
)

}
