% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/simulation_mean.R
\name{ppi_empirical_power}
\alias{ppi_empirical_power}
\title{Monte Carlo Estimation of Empirical and Close-Form Power for PPI / PPI++ mean estimation}
\usage{
ppi_empirical_power(
  R = 2000,
  n,
  N,
  X_sampler_L,
  X_sampler_U = NULL,
  f_generator,
  eps_sampler = function(n) rnorm(n, 0, 1),
  model_type = c("glm_correct", "glm_mis", "glm_wrong", "rf"),
  ppi_type = c("naive", "PPI", "PPI++"),
  lambda_mode = c("plugin", "theory"),
  lambda_external = FALSE,
  n_external = ceiling(n/2),
  alpha = 0.05,
  delta = 0,
  seed = NULL,
  pred_noise = 0,
  PPI_crossfit = FALSE,
  PPIpp_crossfit = TRUE,
  PPIpp_independent_split = FALSE
)
}
\arguments{
\item{R}{Number of Monte Carlo repetitions.}

\item{n}{Size of labeled sample.}

\item{N}{Size of unlabeled sample.}

\item{X_sampler_L}{Function generating labeled covariates.}

\item{X_sampler_U}{Function generating unlabeled covariates (defaults to \code{X_sampler_L}).}

\item{f_generator}{Function generating signal mean function f(x).}

\item{eps_sampler}{Error distribution sampler, default standard normal.}

\item{model_type}{Choice of predictive model: \code{"glm_correct"}, \code{"glm_mis"}, \code{"glm_wrong"}, \code{"rf"}.}

\item{ppi_type}{\code{"naive"}, \code{"PPI"}, or \code{"PPI++"}.}

\item{lambda_mode}{\code{"plugin"} or \code{"theory"}.}

\item{lambda_external}{Whether to estimate lambda on an external set.}

\item{n_external}{Size of external set if used.}

\item{alpha}{Significance level.}

\item{delta}{True mean shift.}

\item{seed}{Optional integer random seed for reproducibility.}

\item{pred_noise}{Optional added prediction noise.}

\item{PPI_crossfit}{Logical, use cross-fitting for PPI.}

\item{PPIpp_crossfit}{Logical, use cross-fitting for PPI++.}

\item{PPIpp_independent_split}{Logical, use 3-way independent split.}
}
\value{
A list containing:
\item{empirical_power}{Monte Carlo rejection rate}
\item{mc_se}{Monte Carlo SE of empirical power}
\item{theoretical_power}{closed-form population power}
\item{avg_lambda_hat}{average estimated \eqn{\hat\lambda}}
\item{cov_var_table}{tibble with covariance summaries used for power formula}
}
\description{
Runs a full Monte Carlo simulation for estimating power for the
naive, PPI, or PPI++ estimators. Returns empirical rejection probability,
Monte Carlo standard error, and theoretical (closed-form) power based
on estimated population-level covariance components.
}
\examples{
ppi_empirical_power(
  R = 500,
  n = 100,
  N = 10000,
  X_sampler_L = function(n) data.frame(x1 = rnorm(n), x2 = rnorm(n)),
  f_generator = function(X) 2 * X$x1,
  model_type = "glm_correct",
  ppi_type = "PPI"
)
}
