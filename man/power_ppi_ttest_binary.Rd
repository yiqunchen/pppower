% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ppi_plus_mean_power.R
\name{power_ppi_ttest_binary}
\alias{power_ppi_ttest_binary}
\title{Power for PPI++ two-sample test with binary outcomes (sens/spec input)}
\usage{
power_ppi_ttest_binary(
  p_A,
  p_B,
  n_A,
  n_B,
  N_A,
  N_B,
  sens_A,
  spec_A,
  sens_B = sens_A,
  spec_B = spec_A,
  delta = NULL,
  alpha = 0.05
)
}
\arguments{
\item{p_A, p_B}{Group-specific prevalences \eqn{P(Y=1)}.}

\item{n_A, n_B}{Labeled sample sizes per group.}

\item{N_A, N_B}{Unlabeled sample sizes per group.}

\item{sens_A, spec_A}{Sensitivity and specificity for group A predictions.}

\item{sens_B, spec_B}{Sensitivity and specificity for group B predictions
(defaults match group A).}

\item{delta}{Optional effect size \eqn{p_A - p_B}. If \code{NULL}, it is
computed from \code{p_A} and \code{p_B}.}

\item{alpha}{Two-sided significance level (default 0.05).}
}
\value{
Scalar power in [0, 1] with a \code{"moments"} attribute containing
the per-group moment lists.
}
\description{
Computes PPI++ power for a two-sample difference in proportions when
predictions come from a binary classifier summarized by sensitivity and
specificity. This helper converts the classification metrics into the
required moment inputs and delegates to \code{power_ppi_ttest()}.
}
