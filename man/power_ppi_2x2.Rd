% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ppi_2x2_power.R
\name{power_ppi_2x2}
\alias{power_ppi_2x2}
\title{Power or required sample size for a 2x2 table with PPI++ surrogates}
\usage{
power_ppi_2x2(
  p_exp,
  p_ctrl,
  N,
  n = NULL,
  power = NULL,
  alpha = 0.05,
  prev_exp = 0.5,
  sens,
  spec,
  effect_measure = c("OR", "RR"),
  lambda_mode = "oracle",
  lambda_user = NULL,
  warn_smallN = TRUE,
  ...
)
}
\arguments{
\item{p_exp}{Numeric scalar. Probability of outcome in the exposed group,
\eqn{P(Y = 1 \mid X = 1)}.}

\item{p_ctrl}{Numeric scalar. Probability of outcome in the control group,
\eqn{P(Y = 1 \mid X = 0)}.}

\item{N}{Positive integer. Total number of unlabeled observations.}

\item{n}{Total number of labeled observations. Set to \code{NULL} to solve for
the required \code{n}.}

\item{power}{Target power. Set to \code{NULL} to compute power from \code{n}.}

\item{alpha}{Significance level (default \code{0.05}).}

\item{prev_exp}{Numeric scalar. Prevalence of exposure, \eqn{P(X = 1)}.
Default \code{0.5} (balanced groups).}

\item{sens}{Sensitivity of the binary surrogate classifier.}

\item{spec}{Specificity of the binary surrogate classifier.}

\item{effect_measure}{Character. \code{"OR"} (odds ratio, default) or \code{"RR"}
(relative risk).}

\item{lambda_mode}{\code{"oracle"} (default), \code{"vanilla"}, or \code{"user"}.}

\item{lambda_user}{Scalar lambda when \code{lambda_mode = "user"}.}

\item{warn_smallN}{Logical. Warn when \code{N} is small.}

\item{...}{Additional arguments (currently unused).}
}
\value{
When computing power: scalar in [0, 1].
When computing sample size: integer \code{n} required.
}
\description{
Computes power or solves for the required labeled sample size for testing
an odds ratio (OR) or relative risk (RR) from a 2x2 contingency table
when a binary PPI++ surrogate is available.

Follows the \pkg{pwr} convention: exactly one of \code{n} or \code{power} must be
\code{NULL}.

\strong{Odds ratio} (default): Uses logistic regression with design matrix
\eqn{[1, X]} where \eqn{X \in \{0,1\}} is the group indicator. The effect
is \eqn{\beta_1 = \log(\text{OR})}. Analytical variance blocks are
computed from the cell probabilities and surrogate performance, then
passed to \code{\link[=power_ppi_regression]{power_ppi_regression()}}.

\strong{Relative risk}: Uses the delta method on per-group PPI++ mean estimates.
The test statistic is a Wald test for \eqn{\log(\hat p_1 / \hat p_0)}.
}
\examples{
# OR test: exposed group has higher risk
power_ppi_2x2(
  p_exp = 0.40, p_ctrl = 0.20,
  N = 500, n = 80, alpha = 0.05,
  sens = 0.85, spec = 0.85,
  effect_measure = "OR"
)

# Solve for required n
power_ppi_2x2(
  p_exp = 0.40, p_ctrl = 0.20,
  N = 500, power = 0.80, alpha = 0.05,
  sens = 0.85, spec = 0.85,
  effect_measure = "OR"
)

# RR test
power_ppi_2x2(
  p_exp = 0.40, p_ctrl = 0.20,
  N = 500, n = 80, alpha = 0.05,
  sens = 0.85, spec = 0.85,
  effect_measure = "RR"
)
}
