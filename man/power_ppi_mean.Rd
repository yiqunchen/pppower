% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ppi_plus_mean_power.R
\name{power_ppi_mean}
\alias{power_ppi_mean}
\title{Power or required sample size for the PPI++ mean estimator}
\usage{
power_ppi_mean(
  delta,
  N,
  n = NULL,
  power = NULL,
  alpha = 0.05,
  sigma_y2 = NULL,
  sigma_f2 = NULL,
  cov_y_f = NULL,
  var_f = NULL,
  var_res = NULL,
  metrics = NULL,
  metric_type = NULL,
  m_labeled = NULL,
  correction = TRUE,
  lambda = NULL,
  lambda_type = c("oracle", "user"),
  lambda_mode = NULL,
  lambda_user = NULL,
  mode = c("error", "cap"),
  warn_smallN = TRUE,
  smallN_threshold = 500
)
}
\arguments{
\item{delta}{Effect size \eqn{\theta - \theta_0}.}

\item{N}{Unlabeled sample size.}

\item{n}{Labeled sample size. Set to \code{NULL} to solve for the required \code{n}.}

\item{power}{Target power. Set to \code{NULL} (default) to compute power from \code{n}.}

\item{alpha}{Two-sided significance level (default 0.05).}

\item{sigma_y2}{Optional outcome variance; overrides anything implied by \code{metrics}.}

\item{sigma_f2}{Optional prediction variance; overrides anything implied by \code{metrics}.}

\item{cov_y_f}{Optional covariance \eqn{\mathrm{Cov}(Y, f(X))}. When supplied (directly
or via \code{metrics$cov_y_f}) the PPI++ power is returned in addition to the PP
quantities.}

\item{var_f}{Optional variance of \eqn{f(X)}.}

\item{var_res}{Optional residual variance of residual \eqn{Y - f(X)}.}

\item{metrics}{Optional list of predictive-performance summaries.}

\item{metric_type}{Character string describing the metric bundle (e.g.,
\code{"continuous"}, \code{"hard"}, \code{"prob"}).}

\item{m_labeled}{Labeled sample size associated with the metrics (defaults to \code{n}).}

\item{correction}{Logical; apply finite-sample variance corrections (default \code{TRUE}).}

\item{lambda}{Optional user-specified blend weight.}

\item{lambda_type}{\code{"oracle"} (default) uses the closed-form blend; \code{"user"}
evaluates power at the supplied \code{lambda}.}

\item{lambda_mode}{Alias for \code{lambda_type} used in sample-size mode;
one of \code{"vanilla"}, \code{"oracle"}, or \code{"user"}.}

\item{lambda_user}{Scalar specifying \eqn{\lambda} when
\code{lambda_mode = "user"} (sample-size mode only).}

\item{mode}{\code{"error"} (default) or \code{"cap"}.
\code{"cap"} returns \code{n = N} with an \code{"achieved_power"} attribute when the
required sample size exceeds \code{N}. Only used when solving for \code{n}.}

\item{warn_smallN}{Logical. Warn when \code{N < smallN_threshold} (default \code{TRUE}).}

\item{smallN_threshold}{Numeric. Default \code{500}.}
}
\value{
When computing power: scalar in [0, 1].
When computing sample size: integer \code{n} required, possibly with
\code{"achieved_power"} attribute if capped.
}
\description{
Unified interface for power analysis of the PPI++ mean estimator.
Follows the \pkg{pwr} convention: supply \strong{either} \code{n} or \code{power} (but not
both). The function computes whichever is left \code{NULL}.
\itemize{
\item \code{power = NULL} (default): compute power given \code{n}.
\item \code{n = NULL}: solve for the minimum labeled sample size achieving \code{power}.
}

You can supply raw variance pieces (\code{sigma_y2}, \code{sigma_f2}, \code{cov_y_f},
\code{var_f}, \code{var_res}) or reuse the metrics interface (\code{metrics},
\code{metric_type}, \code{m_labeled}, \code{correction}) to back out the required inputs.
}
\examples{
# Compute power given n:
power_ppi_mean(delta = 0.3, N = 5000, n = 200,
               sigma_y2 = 1, sigma_f2 = 0.5, cov_y_f = 0.4)

# Compute required n given target power:
power_ppi_mean(delta = 0.3, N = 5000, power = 0.8,
               sigma_y2 = 1, sigma_f2 = 0.5, cov_y_f = 0.4)

}
