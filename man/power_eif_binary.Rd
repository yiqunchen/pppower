% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/eif_binary_power.R
\name{power_eif_binary}
\alias{power_eif_binary}
\title{Power or required calibration size for EIF estimator with binary surrogates}
\usage{
power_eif_binary(
  delta,
  N,
  m_cal = NULL,
  power = NULL,
  alpha = 0.05,
  mu0 = NULL,
  mu1 = NULL,
  p_hat = NULL,
  p = NULL,
  sens = NULL,
  spec = NULL
)
}
\arguments{
\item{delta}{Effect size \eqn{\theta - \theta_0}.}

\item{N}{Unlabeled/test sample size with surrogate predictions.}

\item{m_cal}{Calibration (labeled) sample size. Set to \code{NULL} to solve for
the required \code{m_cal}.}

\item{power}{Target power. Set to \code{NULL} (default) to compute power from
\code{m_cal}.}

\item{alpha}{Two-sided significance level (default 0.05).}

\item{mu0, mu1}{Conditional means \eqn{E[Y \mid \hat Y = 0]} and
\eqn{E[Y \mid \hat Y = 1]}.}

\item{p_hat}{Prevalence of predicted positives \eqn{P(\hat Y = 1)}.}

\item{p}{True prevalence \eqn{P(Y = 1)}.}

\item{sens, spec}{Sensitivity and specificity of the surrogate classifier.}
}
\value{
When computing power: scalar in [0, 1] with attributes \code{theta},
\code{p_hat}, \code{var_unlabeled}, and \code{var_cal}.
When computing sample size: integer required calibration size.
}
\description{
Unified interface for the efficient influence function (EIF) estimator used
with binary surrogate predictions (e.g., LLM-as-a-judge hard votes).
Follows the \pkg{pwr} convention: supply \strong{either} \code{m_cal} or \code{power}
(but not both). The function computes whichever is left \code{NULL}.

You can provide either direct conditional means \verb{(mu0, mu1, p_hat)} or
sensitivity/specificity plus prevalence \verb{(p, sens, spec)}.
}
