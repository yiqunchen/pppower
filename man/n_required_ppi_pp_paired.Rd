% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ppi_plus_mean_power.R
\name{n_required_ppi_pp_paired}
\alias{n_required_ppi_pp_paired}
\title{Required sample size for PPI++ paired t-test}
\usage{
n_required_ppi_pp_paired(
  delta,
  N,
  power = 0.8,
  alpha = 0.05,
  sigma_D2,
  rho_D,
  sigma_fD2 = NULL
)
}
\arguments{
\item{delta}{Effect size: the true mean difference to detect.}

\item{N}{Number of unlabeled pairs with predictions.}

\item{power}{Target power (default 0.80).}

\item{alpha}{Two-sided significance level (default 0.05).}

\item{sigma_D2}{Variance of the differences \eqn{D = Y^A - Y^B}.}

\item{rho_D}{Correlation between \eqn{D} and \eqn{f^D}.}

\item{sigma_fD2}{Optional variance of predicted differences.}
}
\value{
Required number of labeled pairs (scalar).
}
\description{
Computes the required number of labeled pairs for a paired t-test using PPI++
to achieve target power.
}
\examples{
# How many pairs needed to detect delta=0.3 with 80\% power?
n_required_ppi_pp_paired(
  delta = 0.3,
  N = 1000,
  power = 0.80,
  sigma_D2 = 1,
  rho_D = 0.7
)

}
