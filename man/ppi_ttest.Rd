% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ppi_tests.R
\name{ppi_ttest}
\alias{ppi_ttest}
\title{PPI++ Two-Sample t-Test}
\usage{
ppi_ttest(
  Y_A,
  f_A_L,
  f_A_U,
  Y_B,
  f_B_L,
  f_B_U,
  delta0 = 0,
  alpha = 0.05,
  lambda_A = NULL,
  lambda_B = NULL
)
}
\arguments{
\item{Y_A}{Numeric vector of labeled outcomes for group A.}

\item{f_A_L}{Numeric vector of predictions for labeled samples in group A.}

\item{f_A_U}{Numeric vector of predictions for unlabeled samples in group A.}

\item{Y_B}{Numeric vector of labeled outcomes for group B.}

\item{f_B_L}{Numeric vector of predictions for labeled samples in group B.}

\item{f_B_U}{Numeric vector of predictions for unlabeled samples in group B.}

\item{delta0}{Null hypothesis value for the difference (default 0).}

\item{alpha}{Significance level (default 0.05).}

\item{lambda_A}{Optional lambda for group A. If NULL, uses EIF-optimal.}

\item{lambda_B}{Optional lambda for group B. If NULL, uses EIF-optimal.}
}
\value{
A list with components:
\describe{
\item{estimate}{The PPI++ estimate of the difference (mean_A - mean_B).}
\item{se}{Standard error of the difference estimate.}
\item{z_stat}{Z test statistic.}
\item{p_value}{Two-sided p-value.}
\item{reject}{Logical indicating whether null is rejected at level alpha.}
\item{lambda_A}{Lambda used for group A.}
\item{lambda_B}{Lambda used for group B.}
\item{ci}{Confidence interval at level 1-alpha.}
}
}
\description{
Performs a two-sample hypothesis test for the difference in means between
two independent groups using PPI++ estimators.
}
\examples{
set.seed(123)
n <- 50; N <- 500
# Group A (treatment)
Y_A <- rnorm(n, mean = 0.3)
f_A_L <- Y_A + rnorm(n, sd = 0.5)
f_A_U <- rnorm(N, mean = 0.3) + rnorm(N, sd = 0.5)
# Group B (control)
Y_B <- rnorm(n, mean = 0)
f_B_L <- Y_B + rnorm(n, sd = 0.5)
f_B_U <- rnorm(N, mean = 0) + rnorm(N, sd = 0.5)

ppi_ttest(Y_A, f_A_L, f_A_U, Y_B, f_B_L, f_B_U)

}
