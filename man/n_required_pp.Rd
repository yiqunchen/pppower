% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/required_n_ppi.R
\name{n_required_pp}
\alias{n_required_pp}
\title{Required labeled sample size for desired PPI power}
\usage{
n_required_pp(
  delta,
  N,
  alpha = 0.05,
  power = 0.8,
  type = c("mean", "ols", "custom"),
  var_f = NULL,
  var_res = NULL,
  metrics = NULL,
  metric_type = NULL,
  m_labeled = NULL,
  correction = TRUE,
  V_u = NULL,
  V_l = NULL,
  c = NULL,
  var_unlabeled = NULL,
  var_labeled = NULL,
  warn_smallN = TRUE,
  smallN_threshold = 500,
  mode = c("error", "cap")
)
}
\arguments{
\item{delta}{Effect size \eqn{\theta - \theta_0}.}

\item{N}{Unlabeled sample size.}

\item{alpha}{Two-sided significance level.}

\item{power}{Target power (\eqn{1-\beta}).}

\item{type}{\code{"mean"}, \code{"ols"}, or \code{"custom"}.}

\item{var_f}{Optional variance components of \eqn{f(X)}
(used when \code{type = "mean"} and \code{metrics} is \code{NULL}).}

\item{var_res}{Optional variance components of \eqn{Y-f(X)}
(used when \code{type = "mean"} and \code{metrics} is \code{NULL}).}

\item{metrics}{Optional list of predictive-performance summaries (passed to
\code{resolve_ppi_variances()}).}

\item{metric_type}{Character string describing \code{metrics} (e.g. \code{"continuous"},
\code{"hard"}, \code{"prob"}, \code{"precision_recall"}).}

\item{m_labeled}{Labeled sample size associated with \code{metrics}; defaults to
\code{metrics$m_obs} when present.}

\item{correction}{Logical; apply finite-sample variance corrections when
deriving moments from metrics (default \code{TRUE}).}

\item{V_u, V_l}{Sandwich covariance matrices for PP-OLS (required when
\code{type = "ols"}).}

\item{c}{Numeric contrast vector for PP-OLS (same length as the regressors).}

\item{var_unlabeled, var_labeled}{Optional variance components for
\code{type = "custom"} corresponding to \eqn{\Var_u} and \eqn{\Var_l}.}

\item{warn_smallN}{Logical; warn if \code{N < smallN_threshold}.}

\item{smallN_threshold}{Threshold at which the small-\eqn{N} warning fires.}

\item{mode}{\code{"error"} (default) to error when the required \eqn{n} exceeds \code{N},
or \code{"cap"} to return \code{n = N} with the achieved power attached as an attribute.}
}
\value{
Integer labeled sample size meeting the target power; when \code{mode = "cap"}
and the cap is hit, the result carries an \code{"achieved_power"} attribute.
}
\description{
Computes the minimum labeled sample size \eqn{n} that attains a target
two-sided power for the vanilla prediction-powered mean estimator, a PP-OLS
contrast, or a custom variance pair. For the mean case you can supply
\eqn{\Var(f)} and \eqn{\Var(Y-f)} directly or recover them from predictive
metrics via \code{resolve_ppi_variances()}.
}
\examples{
# Mean estimator
n_required_pp(
  delta = 0.1, N = 5000, var_f = 0.2, var_res = 0.5,
  alpha = 0.05, power = 0.8, type = "mean"
)

# PP-OLS contrast
V_u <- diag(c(0.8, 0.6)); V_l <- diag(c(0.5, 0.7))
n_required_pp(
  delta = 0.15, N = 4000, V_u = V_u, V_l = V_l, c = c(1, 0),
  alpha = 0.05, power = 0.85, type = "ols"
)
}
