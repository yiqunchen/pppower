% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ppi_plus_mean_power.R
\name{power_ppi_paired}
\alias{power_ppi_paired}
\title{Power for PPI++ paired t-test (within-subject differences)}
\usage{
power_ppi_paired(
  delta,
  N,
  n = NULL,
  power = NULL,
  alpha = 0.05,
  sigma_D2,
  rho_D,
  sigma_fD2 = NULL
)
}
\arguments{
\item{delta}{Effect size: the true mean difference \eqn{E[Y^A - Y^B]}.}

\item{N}{Number of unlabeled pairs with predictions.}

\item{n}{Number of labeled pairs.}

\item{power}{Target power. Set to \code{NULL} (default) to compute power from \code{n}.
Set to a value in (0, 1) to solve for \code{n} instead.}

\item{alpha}{Two-sided significance level (default 0.05).}

\item{sigma_D2}{Variance of the differences \eqn{D = Y^A - Y^B}.}

\item{rho_D}{Correlation between \eqn{D} and \eqn{f^D}.}

\item{sigma_fD2}{Optional variance of predicted differences. If NULL,
assumed equal to \code{sigma_D2}.}
}
\value{
When \code{power = NULL}: scalar power in \eqn{[0, 1]}.
When \code{n = NULL}: required number of labeled pairs (integer).
}
\description{
Computes power for a paired t-test using PPI++. This is equivalent to
mean estimation on the differences \eqn{D_i = Y_i^A - Y_i^B} with auxiliary
predictions \eqn{f^D_i = f^A(X_i) - f^B(X_i)}.
}
\details{
The paired t-test reduces to a one-sample problem on the differences.
The PPI++ variance for paired data is:
\deqn{\mathrm{Var}(\hat\Delta_{\lambda^*}) \approx \frac{\sigma_D^2(1 - \rho_D^2)}{n}}
where \eqn{\rho_D} is the correlation between the observed difference and
the predicted difference.
}
\examples{
# Compute power
power_ppi_paired(
  delta = 0.3,
  N = 1000,
  n = 50,
  sigma_D2 = 1,
  rho_D = 0.7
)

# Compute required sample size
power_ppi_paired(
  delta = 0.3,
  N = 1000,
  power = 0.80,
  sigma_D2 = 1,
  rho_D = 0.7
)

}
