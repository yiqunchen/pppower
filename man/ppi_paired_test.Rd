% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ppi_tests.R
\name{ppi_paired_test}
\alias{ppi_paired_test}
\title{PPI++ Paired t-Test}
\usage{
ppi_paired_test(D_L, fD_L, fD_U, delta0 = 0, alpha = 0.05, lambda = NULL)
}
\arguments{
\item{D_L}{Numeric vector of labeled differences (Y_A - Y_B).}

\item{fD_L}{Numeric vector of predicted differences for labeled samples.}

\item{fD_U}{Numeric vector of predicted differences for unlabeled samples.}

\item{delta0}{Null hypothesis value for the mean difference (default 0).}

\item{alpha}{Significance level (default 0.05).}

\item{lambda}{Optional lambda. If NULL, uses EIF-optimal.}
}
\value{
A list with components (same as \code{ppi_mean_test}).
}
\description{
Performs a paired hypothesis test for the mean difference using PPI++.
This is equivalent to a one-sample test on the differences D = Y_A - Y_B.
}
\examples{
set.seed(123)
n <- 50; N <- 500
D_L <- rnorm(n, mean = 0.3)  # True difference
fD_L <- D_L + rnorm(n, sd = 0.5)
fD_U <- rnorm(N, mean = 0.3) + rnorm(N, sd = 0.5)
ppi_paired_test(D_L, fD_L, fD_U)

}
