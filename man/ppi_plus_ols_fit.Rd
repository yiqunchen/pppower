% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ppi_plus_ols_fit.R
\name{ppi_plus_ols_fit}
\alias{ppi_plus_ols_fit}
\title{Fit PPI++ OLS with sandwich variability}
\usage{
ppi_plus_ols_fit(
  y,
  f_col,
  formula,
  data_l,
  data_u,
  lambda = NULL,
  lambda_type = c("plugin", "user"),
  contrast = NULL,
  clip = TRUE,
  tol = 1e-12
)
}
\arguments{
\item{y}{Outcome column name in \code{data_l}.}

\item{f_col}{Column name with predictions in both data sets.}

\item{formula}{RHS-only formula for covariates.}

\item{data_l}{Labeled data frame.}

\item{data_u}{Unlabeled data frame.}

\item{lambda}{Optional blend weight when \code{lambda_type = "user"}.}

\item{lambda_type}{\code{"plugin"} (default) chooses lambda via the plug-in lambda\eqn{^\star}
for a supplied \code{contrast}; \code{"user"} uses the provided \code{lambda}.}

\item{contrast}{Numeric vector (length equals number of regressors) specifying
the contrast used for lambda estimation when \code{lambda_type = "plugin"}.}

\item{clip}{Logical; if \code{TRUE}, clips lambda to \eqn{[0,1]}.}

\item{tol}{Numerical tolerance for linear solves.}
}
\value{
An object of class \code{c("ppi_plus_ols_fit", "ppi_ols_fit")} containing
coefficients, covariance, standard errors, lambda, and moment pieces.
}
\description{
Fit PPI++ OLS with sandwich variability
}
